name = "agent"
tier = "standard"
kind = "session"
version = "0.1.0"
help = "session-feat stub: drives zish's executor over the v0 frame protocol"
bin = "agent"
usage = "agent   # runs a canned round-trip (no model yet)"
