name = "agent"
tier = "standard"
kind = "session"
version = "0.3.0"
help = "model loop over the session frame protocol; also `agent edit` — a stdin→stdout region filter for editors"
bin = "agent"
usage = "agent [-m model] <query>  |  agent edit \"<instruction>\" < region   # key at ~/.zish/openrouter.key (0600)"
